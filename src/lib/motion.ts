/**
 * App-wide motion: CSS variables (`globals.css` :root) + composed Tailwind class strings.
 * Import from here for consistent transitions and entrance animations.
 */
export * from './animation-system'
