import { SvglBrandLogo } from '@/shared/components/svgl-brand-logo'

export function GoogleWorkspaceIcon({ className }: { className?: string }) {
  return <SvglBrandLogo brand="google" className={className} labeled={false} />
}
