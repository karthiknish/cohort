'use client'

import { Briefcase, ChevronDown, ChevronUp, MapPin, Plus, RefreshCw, Settings2, Target, Users } from 'lucide-react'

import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { cn } from '@/lib/utils'

import type { Insights, TargetingData } from './audience-targeting-card'

export function AudienceTargetingDisconnectedState({ providerName }: { providerName: string }) {
  return <Card><CardHeader><CardTitle className="flex items-center gap-2 text-lg"><Target className="h-5 w-5" />Audience Targeting</CardTitle><CardDescription>Connect {providerName} to view targeting criteria</CardDescription></CardHeader></Card>
}

export function AudienceTargetingHeader({ insights, loading, onLoadTargeting, onOpenBuilder, providerName }: { insights: Insights | null; loading: boolean; onLoadTargeting: () => void; onOpenBuilder: () => void; providerName: string }) {
  return <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2"><div><CardTitle className="flex items-center gap-2 text-lg"><Target className="h-5 w-5" />Audience Targeting</CardTitle><CardDescription>{insights ? `${insights.totalEntities} targeting configs` : `View ${providerName} audience targeting`}</CardDescription></div><div className="flex gap-2"><Button variant="outline" size="sm" onClick={onOpenBuilder} disabled={loading}><Plus className="mr-2 h-4 w-4" />Create Audience</Button><Button variant="outline" size="sm" onClick={onLoadTargeting} disabled={loading}><RefreshCw className={cn('mr-2 h-4 w-4', loading && 'animate-spin')} />Load Targeting</Button></div></CardHeader>
}

function AudienceTargetingSummary({ insights }: { insights: Insights }) {
  return <div className="grid grid-cols-3 gap-4 rounded-lg bg-muted/50 p-4"><div className="flex items-center gap-2"><Users className="h-4 w-4 text-muted-foreground" /><div><p className="text-sm font-medium">{insights.audienceStats.totalAudiences}</p><p className="text-xs text-muted-foreground">Audiences</p></div></div><div className="flex items-center gap-2"><MapPin className="h-4 w-4 text-muted-foreground" /><div><p className="text-sm font-medium">{insights.demographicCoverage.hasLocationTargeting ? 'Yes' : 'No'}</p><p className="text-xs text-muted-foreground">Geo Targeting</p></div></div><div className="flex items-center gap-2"><Briefcase className="h-4 w-4 text-muted-foreground" /><div><p className="text-sm font-medium">{insights.interestStats.totalInterests}</p><p className="text-xs text-muted-foreground">Interests</p></div></div></div>
}

function TargetingAccordionItem({ expandedId, formatAgeRange, onEdit, onToggleExpanded, targeting }: { expandedId: string | null; formatAgeRange: (range: string) => string; onEdit: (entityId: string) => void; onToggleExpanded: (entityId: string) => void; targeting: TargetingData }) {
  const isExpanded = expandedId === targeting.entityId
  return <div className="rounded-lg border"><button type="button" className="flex w-full items-center justify-between p-3 transition-colors hover:bg-muted/50" onClick={() => onToggleExpanded(targeting.entityId)}><div className="flex items-center gap-2"><span className="font-medium">{targeting.entityName || targeting.entityId}</span><Badge variant="outline" className="text-xs capitalize">{targeting.entityType}</Badge></div>{isExpanded ? <ChevronUp className="h-4 w-4" /> : <div className="flex items-center gap-2"><Button type="button" variant="ghost" size="sm" className="h-7 px-2 text-xs" onClick={(event) => { event.stopPropagation(); onEdit(targeting.entityId) }}><Settings2 className="mr-1 h-3.5 w-3.5" />Edit</Button><ChevronDown className="h-4 w-4" /></div>}</button>{isExpanded ? <div className="space-y-3 p-4 pt-0 text-sm">{(targeting.demographics.ageRanges.length > 0 || targeting.demographics.genders.length > 0) ? <div><p className="mb-1 text-xs font-medium text-muted-foreground">Demographics</p><div className="flex flex-wrap gap-1">{targeting.demographics.ageRanges.map((age) => <Badge key={`${targeting.entityId}-age-${age}`} variant="secondary" className="text-xs">{formatAgeRange(age)}</Badge>)}{targeting.demographics.genders.map((gender) => <Badge key={`${targeting.entityId}-gender-${gender}`} variant="secondary" className="text-xs capitalize">{gender.toLowerCase()}</Badge>)}</div></div> : null}{targeting.locations.included.length > 0 ? <div><p className="mb-1 text-xs font-medium text-muted-foreground">Locations</p><div className="flex flex-wrap gap-1">{targeting.locations.included.slice(0, 5).map((location) => <Badge key={`${targeting.entityId}-loc-${location.name}`} variant="outline" className="text-xs">{location.name}</Badge>)}{targeting.locations.included.length > 5 ? <Badge variant="outline" className="text-xs">+{targeting.locations.included.length - 5} more</Badge> : null}</div></div> : null}{targeting.audiences.included.length > 0 ? <div><p className="mb-1 text-xs font-medium text-muted-foreground">Audiences</p><div className="flex flex-wrap gap-1">{targeting.audiences.included.slice(0, 5).map((audience) => <Badge key={`${targeting.entityId}-aud-${audience.id ?? audience.name}`} className="text-xs">{audience.name}</Badge>)}{targeting.audiences.included.length > 5 ? <Badge className="text-xs">+{targeting.audiences.included.length - 5} more</Badge> : null}</div></div> : null}{targeting.professional && (targeting.professional.industries.length > 0 || targeting.professional.jobTitles.length > 0) ? <div><p className="mb-1 text-xs font-medium text-muted-foreground">Professional</p><div className="flex flex-wrap gap-1">{targeting.professional.industries.slice(0, 3).map((industry) => <Badge key={`${targeting.entityId}-industry-${industry.id ?? industry.name}`} variant="outline" className="text-xs">{industry.name}</Badge>)}{targeting.professional.jobTitles.slice(0, 3).map((job) => <Badge key={`${targeting.entityId}-job-${job.id ?? job.name}`} variant="outline" className="text-xs">{job.name}</Badge>)}</div></div> : null}</div> : null}</div>
}

export function AudienceTargetingContent({ expandedId, formatAgeRange, insights, onEdit, onToggleExpanded, targeting }: { expandedId: string | null; formatAgeRange: (range: string) => string; insights: Insights | null; onEdit: (entityId: string) => void; onToggleExpanded: (entityId: string) => void; targeting: TargetingData[] }) {
  if (targeting.length === 0) return <CardContent><p className="text-sm text-muted-foreground">Click &quot;Load Targeting&quot; to view audience targeting data.</p></CardContent>
  return <CardContent><div className="space-y-4">{insights ? <AudienceTargetingSummary insights={insights} /> : null}<div className="max-h-[400px] space-y-2 overflow-auto">{targeting.map((item) => <TargetingAccordionItem key={item.entityId} expandedId={expandedId} formatAgeRange={formatAgeRange} onEdit={onEdit} onToggleExpanded={onToggleExpanded} targeting={item} />)}</div></div></CardContent>
}