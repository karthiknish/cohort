'use client';
import { AgentModeDynamic } from '@/shared/components/agent-mode/agent-mode-dynamic';
/** Agent overlay for For You routes (same workspace context as dashboard). */
export function ForYouAgentMode() {
    return <AgentModeDynamic />;
}
